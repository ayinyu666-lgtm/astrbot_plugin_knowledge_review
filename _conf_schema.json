{
  "webui_port": {
    "description": "审核 WebUI 端口 / Review WebUI port",
    "type": "int",
    "hint": "默认 8095，修改后需重启插件 / Default 8095, restart plugin after change",
    "default": 8095
  },
  "webui_host": {
    "description": "WebUI 绑定地址 / WebUI bind address",
    "type": "string",
    "hint": "0.0.0.0 允许外部访问 / allows external; 127.0.0.1 仅本机 / localhost only",
    "default": "0.0.0.0"
  },
  "astr_base_url": {
    "description": "AstrBot HTTP API 地址 / AstrBot HTTP API URL",
    "type": "string",
    "hint": "例如 / e.g. http://localhost:6185",
    "default": "http://localhost:6185"
  },
  "astr_token": {
    "description": "AstrBot API Token（如已启用鉴权）/ AstrBot API Token (if auth enabled)",
    "type": "string",
    "hint": "在 AstrBot 设置中查找 / Find in AstrBot settings",
    "default": ""
  },
  "auto_classify": {
    "description": "自动 AI 分类 / Auto AI classification on ingest",
    "type": "bool",
    "hint": "开启后每条新候选自动分类，消耗少量 tokens / Auto-classifies new candidates, uses some tokens",
    "default": true
  },
  "gate_provider_id": {
    "description": "AI 分类使用的 provider ID / Classifier provider ID",
    "type": "string",
    "hint": "运行 /kr_providers 查看可用列表，留空使用默认 / Run /kr_providers to list; leave empty for default",
    "default": ""
  },
  "fallback_provider_ids": {
    "description": "回退 provider ID 列表 / Fallback provider ID list",
    "type": "list",
    "items": {"type": "string"},
    "hint": "gate_provider_id 不可用时逐个尝试 / Tried in order when gate unavailable. /kr_providers 查看ID",
    "default": []
  },
  "auto_extract_enabled": {
    "description": "算法自动提取知识 QA 对 / Auto-extract knowledge QA pairs from chat",
    "type": "bool",
    "hint": "开启后自动检测群聊中的问答对并生成候选知识 / Detects QA pairs in group chat and creates candidates",
    "default": true
  },
  "extractor_history_size": {
    "description": "提取器每群缓存消息条数 / Extractor message buffer size per group",
    "type": "int",
    "hint": "越大越能检测到间隔较远的问答对 / Larger = detects QA pairs with more messages in between",
    "default": 30
  },
  "extractor_qa_max_gap_sec": {
    "description": "问答对最大时间间隔（秒）/ Max time gap between Q and A (seconds)",
    "type": "int",
    "hint": "超过此间隔的问答不会被配对 / QA pairs beyond this gap are ignored",
    "default": 300
  },
  "extractor_min_answer_len": {
    "description": "最小回答长度 / Minimum answer length",
    "type": "int",
    "hint": "回答文本长度低于此值不会被采纳 / Answers shorter than this are skipped",
    "default": 10
  },
  "extractor_cooldown_sec": {
    "description": "同一 QA 对去重冷却（秒）/ Dedup cooldown for same QA pair (seconds)",
    "type": "int",
    "hint": "防止相同问答对短时间内重复生成候选 / Prevents duplicate candidates for same QA pair",
    "default": 300
  }
}
